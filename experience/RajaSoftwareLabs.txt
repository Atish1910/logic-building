# RSL - Raja Software Labs:

    1. all combinations in sorted order for String.
        
        let string_01 = "abcd";

        // Op => "a ab abc abcd ac acd ad b bc bcd bd c cd d"; 

        // Using Recursion
        function getCombinations (string_01, current = "", index = 0){
            if (current.length>0){
                console.log(current);
            }
            for (let i = index; i<string_01.length; i++){
                getCombinations(string_01, current + string_01[i], i+1);
            }
        }

        getCombinations(string_01);

    2. 