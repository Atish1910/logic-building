# Event Propogation
# Event Bubbling
# Event Loops
# Micro Task and Macro Task
# Why it is called micro task or macro task ? how to defined the given operation is Microtask or macrotask.
# Differenec between useMemo and useCallback
# Write an code for Debouncing in JS.
# What is Output:
        console.log("A");

        setTimeout(() => { 
        console.log("B"); 
        }, 0);

        Promise.resolve()
        .then(() => { console.log("C"); })
        .then(() => { console.log("D"); });

        setTimeout(() => {
        Promise.resolve().then(() => { console.log("E"); });
        console.log("F");
        }, 0);

        console.log("G");