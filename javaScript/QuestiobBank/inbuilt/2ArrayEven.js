// 2. Write a JavaScript function that takes an array of numbers and returns a new array with only the even numbers.

let iArr = [10,11,2,12,13,14,15,16,17,18,19,10];
iEvent = iArr.filter((i) => i % 2 == 0 );
console.log(`${iArr} :  array with only the even numbers =>   ${iEvent}`);
